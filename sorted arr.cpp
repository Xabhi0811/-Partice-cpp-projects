class Solution{	
	public:
		bool isSorted(vector<int>& nums){
			 int n = nums.size();
               if(nums[i] > nums[i + 1]){
                return false;
               
             }
             return true;
		}
};